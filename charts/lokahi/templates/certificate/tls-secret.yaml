{{- if .Values.customDomain.enabled }}
apiVersion: v1
kind: Secret
metadata:
  name: root-ca-certificate
  namespace: {{ .Release.Namespace }}
  annotations:
    "helm.sh/resource-policy": keep
type: kubernetes.io/tls
data: # 
  tls.crt: |-
     {{ .Files.Get "{{ .Values.customDomain.crt }}" | b64enc }}
  tls.key: |-
     {{ .Files.Get "{{ .Values.customDomain.key }}" | b64enc }}
{{- end }}