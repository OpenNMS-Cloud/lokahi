apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.OpenNMS.events.serviceName }}
  labels:
    app: {{ .Values.OpenNMS.events.serviceName }}
  namespace: {{ .Release.Namespace }}
spec:
  type: {{ .Values.OpenNMS.events.serviceType }}
  ports:
    - port: {{ index .Values.OpenNMS.events.ports 0 "containerPort" }}
      protocol: TCP
      name: http
    - port: {{ index .Values.OpenNMS.events.ports 1 "containerPort" }}
      protocol: TCP
      name: grpc
  selector:
    app: {{ .Values.OpenNMS.events.serviceName }}
