apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.OpenNMS.notification.serviceName }}
  labels:
    app: {{ .Values.OpenNMS.notification.serviceName }}
  namespace: {{ .Release.Namespace }}
spec:
  type: {{ .Values.OpenNMS.notification.serviceType }}
  ports:
    - port: {{ index .Values.OpenNMS.notification.ports 1 "containerPort" }}
      protocol: TCP
      name: grpc
  selector:
    app: {{ .Values.OpenNMS.notification.serviceName }}
