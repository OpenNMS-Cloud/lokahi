fragment ChartTimeSeriesMetric on Query {
  metric: metric(
    name: $name
    labels: {
      monitor: $monitor
      node_id: $nodeId
      instance: $instance
      if_name: $ifName
    }
    timeRange: $timeRange
    timeRangeUnit: $timeRangeUnit
  ) {
    ...MetricParts
  }
}
